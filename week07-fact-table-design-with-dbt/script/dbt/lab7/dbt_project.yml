name: 'lab7'
version: '1.0.0'
config-version: 2

profile: 'lab7'

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
macro-paths: ['macros']

clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  staging:
    +materialized: view
  dimensions:
    +materialized: table
  facts:
    +materialized: table
