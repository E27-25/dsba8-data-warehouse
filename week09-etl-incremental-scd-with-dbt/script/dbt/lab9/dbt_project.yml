name: 'lab9'
version: '1.0.0'
config-version: 2

profile: 'lab9'

model-paths: ['models']
seed-paths: ['seeds']
snapshot-paths: ['snapshots']
test-paths: ['tests']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  lab9:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +schema: marts

snapshots:
  lab9:
    +target_schema: snapshots
