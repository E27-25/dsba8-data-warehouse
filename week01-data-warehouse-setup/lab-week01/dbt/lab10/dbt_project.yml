name: lab10
version: "1.0.0"
config-version: 2

profile: lab10

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  lab10:
    staging:
      +materialized: view
    marts:
      +materialized: table
